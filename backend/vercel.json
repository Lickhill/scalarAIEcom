{
    "version": 2,
    "buildCommand": "npm install && npm run build",
    "env": {
        "NODE_ENV": "production"
    },
    "builds": [
        {
            "src": "start.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "prisma/**",
                    ".env.local"
                ]
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "start.js"
        }
    ]
}